const initialState = {
    currentUser: ''
}

export const reducer = (state = initialState, {type, payload})=>{
switch (type) {
    case value:
        
        break;

    default:
        break;
}
}